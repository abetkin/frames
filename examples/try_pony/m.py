_, [tr, *] = yielded

# rv, [tr, *] = yield 'mod', 'SQLTranslator'
# tr = yielded[1][0]
print('tr', tr)


def tree(self):
    yielded = await any(call(), call())
    if yielded.index == 0:
        1 
